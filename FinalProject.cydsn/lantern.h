#include <project.h>

const uint8_t sineBrightness[256];          // sine wave table for modulating brightness
void lightLantern(uint8_t mode);            // function to draw the lantern pattern on screens
